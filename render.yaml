services:
  - type: web
    name: futsal-app
    env: native
    region: frankfurt
    plan: free
    buildCommand: ./gradlew build
    startCommand: java -jar build/libs/*.jar
    envVars:
      - key: JAVA_VERSION
        value: 21